import 'package:flutter/material.dart';

class AppColors {
  // Brand Primary & Accents
  static const Color primary = Color(0xFF059669); // Emerald Leaf
  static const Color primaryLight = Color(0xFF34D399);
  static const Color primaryDark = Color(0xFF065F46);
  static const Color primarySoft = Color(0xFFECFDF5);

  // Secondary Accents
  static const Color accentAmber = Color(0xFFF59E0B);
  static const Color accentAmberSoft = Color(0xFFFEF3C7);
  static const Color accentOrange = Color(0xFFEA580C);

  // Neutral Backgrounds & Surfaces
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;
  static const Color surfaceMuted = Color(0xFFF1F5F9);

  // Typography & Content
  static const Color textPrimary = Color(0xFF0F172A); // Slate 900
  static const Color textSecondary = Color(0xFF64748B); // Slate 500
  static const Color textMuted = Color(0xFF94A3B8); // Slate 400

  // Borders & Dividers
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderFocused = Color(0xFF059669);

  // Status & Feedback
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color errorSoft = Color(0xFFFEE2E2);
  static const Color warning = Color(0xFFF59E0B);

  // Role Badges
  static const Color roleCustomer = Color(0xFF059669);
  static const Color roleCustomerSoft = Color(0xFFD1FAE5);
  static const Color roleShopOwner = Color(0xFFD97706);
  static const Color roleShopOwnerSoft = Color(0xFFFEF3C7);
  static const Color roleAdmin = Color(0xFF4F46E5);
  static const Color roleAdminSoft = Color(0xFFE0E7FF);
}
